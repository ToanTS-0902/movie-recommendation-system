# Dataset

Download a MovieLens dataset from [GroupLens](https://grouplens.org/datasets/movielens/) and place the extracted files here as:

- `movie.csv`, containing `movieId`, `title`, and optionally `genres`.
- `rating.csv`, containing `userId`, `movieId`, `rating`, and optionally `timestamp`.

The raw dataset is intentionally excluded from Git history.
